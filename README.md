# 🛒 vprok-parser

Парсер товаров сайта [vprok.ru](https://www.vprok.ru) с использованием **Puppeteer**.

Скрипт делает следующее:

* 📍 Выбирает регион на сайте
* 📸 Делает полноразмерный скриншот страницы товара (`screenshot.jpg`)
* 💰 Извлекает цену, старую цену (если есть)
* ⭐ Получает рейтинг и количество отзывов
* 💾 Сохраняет данные в `product.txt`

---

## 🚀 Установка

Склонируйте репозиторий и установите зависимости:

```bash
git clone https://github.com/l9mur4ik/vprok-parser.git
cd vprok-parser
npm install
```

> 💡 **Важно:** Node.js >= 18, npm >= 9

---

## ▶️ Запуск проекта

### Через `npm start` (рекомендуемый способ)

В `package.json` добавлен скрипт `start`, поэтому можно запускать так:

```bash
npm start <URL_товара> "<Регион>"
```

Пример:

```bash
npm start https://www.vprok.ru/product/makfa-makfa-izd-mak-spirali-450g--306739 "Владимирская обл."
```

---

### Через прямой вызов Node.js

```bash
node src/index.js <URL_товара> "<Регион>"
```

Пример:

```bash
node src/index.js https://www.vprok.ru/product/makfa-makfa-izd-mak-spirali-450g--306739 "Владимирская обл."
```

---

## 📂 Результат работы

После выполнения скрипта в папке проекта появятся:

* `screenshot.jpg` — скриншот страницы товара
* `product.txt` — данные товара в формате:

```
price=86
priceOld=N/A
rating=4.8
reviewCount=120
```

---

## 🛠 Линтинг и форматирование кода

Для проверки качества кода с ESLint:

```bash
npm run lint
```

Для автоформатирования кода с Prettier:

```bash
npm run format
```

---

## 📌 Игнорируемые файлы

`.gitignore` настроен на исключение:

* `node_modules/`
* скриншоты и выходные файлы (`screenshot.jpg`, `product.txt`, `error.jpg`)
* IDE-конфиги (`.vscode/`, `.idea/`)
* временные файлы Puppeteer


---

## 💡 Советы

* Перед запуском убедитесь, что выбранный регион корректно отображается на сайте.
* Скрипт автоматически ждёт появления цены и закрытия модального окна выбора региона.